const multer = require("multer")


exports.documents = async (files) => {
    console.log(files)
    // const storage = multer.memoryStorage()
    // const upload = multer({ storage: storage })
    // return upload
}


